name: F1 Controller — Build Windows & Android Releases

on:
  push:
    branches: [main]
  workflow_dispatch:  # Manual trigger from GitHub UI

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - run: flutter pub get
      - run: flutter build apk --release --target-platform android-arm64
      - uses: actions/upload-artifact@v4
        with:
          name: f1-controller-android-apk
          path: build/app/outputs/flutter-apk/app-release.apk

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - run: flutter pub get
      - run: flutter build windows --release
      - name: Bundle companion_server with executable
        run: Copy-Item -Path companion_server -Destination build/windows/x64/runner/Release/companion_server -Recurse
      - uses: actions/upload-artifact@v4
        with:
          name: f1-controller-windows-exe
          path: build/windows/x64/runner/Release/
